{
  "name": "xbrowser",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "node --experimental-modules",
    "dev": "cross-env NODE_ENV=development nodemon --experimental-modules",
    "dev:inspect": "cross-env NODE_ENV=development nodemon --inspect --experimental-modules"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "dotenv-expand": "^5.1.0",
    "nodemon": "^2.0.3",
    "puppeteer": "^10.4.0"
  },
  "nodemonConfig": {
    "ignore": [
      "node_modules/**",
      "userData/**",
      "cache/**",
      "extensions/**",
      "*.md"
    ]
  }
}
